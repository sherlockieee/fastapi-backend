# fastapi-backend

To start the project locally:

If this is your first time:
```
$ pip install pipenv
$ pipenv shell
$ pipenv install
```

Start the database:
```
$ pipenv run db
```


In a new terminal, start the server:
```
$ pipenv run dev
```